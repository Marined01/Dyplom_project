POSTGRES_DB=coursework_db
POSTGRES_USER=coursework
POSTGRES_PASSWORD=your-password-here

DB_HOST=localhost
DB_PORT=5432

SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
# Для телефону в тій самій Wi‑Fi мережі додай IP компʼютера, напр. 192.168.1.42

# Опційно: автоматичне створення адміна при старті Docker (docker/entrypoint.sh)
# ADMIN_EMAIL=admin@uni.ua
# ADMIN_PASSWORD=change-me-in-production
# ADMIN_NAME=Admin
# ADMIN_SURNAME=User

# Опційно: аудиторії при старті Docker (номери через кому, без дублікатів)
# SEED_AUDITORIES=101,102,103,201,202
